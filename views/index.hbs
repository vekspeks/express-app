{{#each items}}
  
    <div class="flex text-white bg-teal-700 text-xl font-bold p-4 mb-2 rounded">
        <div class="flex-1">{{name}}</div>
        <div>
        <a class="py-1 px-2 bg-white text-teal-700 rounded hover:bg-slate-200 text-sm font-normal"
        href="/rooms/usage/{{id}}">
        Pregled upotrebe
        </a>
        </div>
    </div>

{{/each}}